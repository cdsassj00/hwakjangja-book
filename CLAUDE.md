# CLAUDE.md — 이 저장소에서 Claude Code가 따를 지침

## 이 저장소가 하는 일
비개발자용 실용 교양서 **『확장자의 재발견』**의 원고 저장소다. 낯선 파일 확장자를 하나씩 열어 정체를 밝히고, 그 구조로 **무엇을 자동화할 수 있는지**, 그 일을 **AI에게 어떻게 시키는지**까지 다룬다. (자매편: 『브라우저의 재발견』)

이 저장소는 위키독스와 연동된다: **`main`에 push하면 위키독스 책에 자동 반영**된다. 규칙은 [github-wikidocs](https://github.com/pahkey/github-wikidocs)를 따른다.

## 저장소 구조 · 규칙
- `TOC.md` — 목차(위키독스 페이지 계층). 모든 페이지를 여기 등록해야 노출됨. `*` 리스트 + 2칸 들여쓰기, 제목은 페이지 H1과 정확히 일치.
- `pages/NN-slug.md` — 각 장. 번호 접두사 + kebab-case. H1은 `# NN. 제목`.
- `assets/` — 이미지. 페이지에서 `../assets/파일.png`로 참조(현재 검증된 경로).
- 상세 작성 규칙은 `AGENTS.md` 참고(제목/리스트/코드블록/이미지 규약).
- 위키독스 박스는 `[[TIP("제목")]] … [[/TIP]]`만 안전하게 지원됨. 그 외 콜아웃은 인용문/표로.

## 책의 톤·챕터 템플릿 (모든 장 공통)

**이건 책이다. 자료집도 슬라이드도 아니다.** 독자는 문단을 따라 읽는다. 훑지 않는다.

- 대상: **비개발자**. 문체 합니다체. 짧은 문장을 쓰되 **문단으로 흐르게** 쓴다. 비유로 원리를 대체하지 않는다. **보장하지 않는 것**을 반드시 명시한다.
- **산문이 기본값이다.** 표·목록·박스는 산문으로 쓰면 오히려 읽기 나쁜 정보에만 쓴다. 설명할 수 있으면 문장으로 쓴다.
- 한 장당 상한: **표 1개, `[[TIP]]` 박스 1개(실습), 코드블록 2~4개(AI 지시문), 소제목 5~6개.** 넘으면 자료집이 된다. 코드블록 4번째는 아래 "만드는 쪽" 지시문에만 허용한다.

### 읽는 쪽과 만드는 쪽 (2026-08-09 추가, 중요)
초고는 자동화가 전부 **"받은 파일을 처리하는"** 쪽에 몰려 있었다. 서문은 바이브코딩을 내세우는데 각 장은 남이 만든 파일만 다루니 4부(HTML·CSS·JS)가 앞의 부들과 겉돌았다.
**각 장은 두 방향을 모두 다룬다.**
1. **읽는 쪽** — 받은 파일에서 값을 꺼내고, 바꾸고, 검사한다. (기존)
2. **만드는 쪽** — 내가 만든 도구가 이 형식으로 **저장하고 발행한다.** 파일 형식이 "읽는 대상"에서 "내가 짓는 재료"로 바뀌는 지점이다.

만드는 쪽의 대표 각도:
- JSON — 도구의 **상태 저장**. 좌석배치도를 만들고 내려받아 다음에 이어서 작업, 동료에게 보내면 그대로 열림. (미리캔버스·파워포인트가 장표를 기억하는 방식과 같다)
- XML — **RSS로 발행**. 사내 공지가 그 순간부터 데이터가 되어 세고 고르고 묶을 수 있게 된다.
- CSV·MD·SVG·ICS·VCF·HTML — 도구가 결과물을 그 형식으로 **내보내기**.

**단, 모든 형식에 억지로 넣지 않는다.** 형식마다 만들 수 있는지가 실제로 갈린다.
- 강함(브라우저에서 바로 만듦): json, csv, svg, md, html, ics, vcf, yaml, xml, jsonl
- 가능(압축·이미지 처리로): hwpx, docx, xlsx, epub, odf, zip, pdf, webp
- 어려움: mp4, mp3, raw, gguf, safetensors, apk, iso, 옛 포맷 → **"이건 읽는 쪽입니다"라고 분명히 말해 준다.** 못 하는 일을 하라고 하는 것이 더 나쁘다.
- 금지: 신상명세 표, 사람 vs 구조 비교표, 「3줄 요약」 같은 개조식 마무리, 장식용 인용구 남발. 요약이 필요하면 마지막 문단이 그 일을 한다.
- 흐름(형식이 아니라 서사): 마주친 장면 → 정체를 한 문장으로 → 직접 열어보기 → 그래서 무엇을 자동화하나 → AI에게 시키는 법과 한계 → 같은 열쇠로 열리는 다음 문.
- **독자 등급은 "완전 초보 일반인"이다.** 사무직 직장인이 읽는다. 전문용어는 처음 나올 때 반드시 한 문장으로 풀고, 영어 약어에는 우리말 설명을 붙인다. "아시다시피", "당연히" 같은 전제 금지. 어려운 개념이 나오면 그 자리에서 예를 든다.
- **모든 장이 반드시 답해야 하는 세 가지** (빠뜨리면 미완성):
  1. **무엇으로 열고 무엇으로 고치나** — 그 확장자를 열람·편집할 수 있는 프로그램. 유료만 말하지 말고 무료·기본 내장 수단을 꼭 포함한다. 초보자가 이 문단만 읽고 당장 파일을 열 수 있어야 한다.
  2. **이 확장자로 어떤 업무자동화를 할 수 있나** — 직무 사례로.
  3. **주로 어떤 결과물을 만들고, 그중 AI에게 넘길 수 있는 지점이 어디인가** — 사람이 판단할 부분과 AI가 반복할 부분을 갈라 준다.
- **자동화 절은 이 책의 심장이다.** "이런 걸 할 수 있습니다" 같은 일반론으로 끝내지 않는다. 독자는 사무직 직장인이므로 **직무와 산출물을 이름으로 부른다** — 인사팀의 재직증명서 200장, 총무팀의 위촉장, 회계팀의 부서별 예산 취합, 영업팀의 지점 실적 보고서, 기획팀의 제안서 사명 일괄 교체처럼. 사례는 장마다 2~3개면 충분하되 반드시 구체적이어야 한다.

### 자동화 사례 검사표 (하나라도 빠지면 다시 쓴다)
1. **누가** — 부서와 직급/이름까지. "담당자"가 아니라 "총무팀 김 대리".
2. **언제·얼마나** — 숫자를 넣는다. "매주 금요일", "신입 40명", "이백 쪽 교재 서른 부".
3. **지금 어떻게 하는가** — 현재의 손작업을 동작 단위로 묘사한다.
4. **무엇이 잘못되는가** — 실제로 나는 사고. "서른 번째부터 앞사람 이름이 남는다", "내선 0105가 69로 안내된다".
5. **구조를 알면 어떻게 바뀌는가** — 사람 몫과 AI 몫을 갈라서.

금지 표현: "~을 생각해 보십시오", "~할 수 있습니다"로 끝나는 사례, 부서 이름 없는 사례, 숫자 없는 사례.
**비전공자가 실제로 그 확장자를 만나는 장면이 떠오르지 않으면 그 사례는 실패다.** 형식 자체가 사무직과 접점이 적다면(예: YAML) 억지로 일반론을 쓰지 말고, "받아서 고치는 상황"처럼 실제로 마주치는 각도를 찾아 쓴다.
- 기준 구현: `pages/01-hwpx.md`. 새 장은 이 **밀도와 문체**를 따른다(구조를 기계적으로 복사하지 않는다).

## 100개 확장자 4개 부(部) 계획
1. 숨은 정체가 반전인 것: hwpx, docx, xlsx, pptx, epub, apk, jar, ipa (=ZIP+XML/묶음)
2. 일상에서 마주치는 낯선 것: svg, md, yaml, toml, ics, vcf, torrent, iso, csv
3. AI·데이터 시대 포맷: json, jsonl, parquet, gguf, safetensors, ipynb, webp, avif
4. 미디어·창작 포맷: heic, raw(cr2/nef), flac, webm, gltf/glb, psd

## 진행 상태
- [x] 01. HWPX (pages/01-hwpx.md, assets/hwpx-structure.png) — 2026-08-08 산문 중심으로 전면 리라이트
- [x] 02. DOCX · XLSX · PPTX (pages/02-docx-xlsx-pptx.md) — 2026-08-08
  - 집필 전 python-docx·openpyxl·pptx와 Excel COM으로 실제 파일을 만들어 내부를 확인했다. 검증 스크립트는 세션 스크래치패드에 있었고 저장소에는 넣지 않았다.
  - 확인된 사실: 오피스는 `mimetype`이 없고 `[Content_Types].xml`을 쓰며 항목 순서 제약이 없다. 엑셀은 문자열을 `xl/sharedStrings.xml`에 모아 두고 셀에는 번호만 넣는다(같은 글자를 쓴 두 셀이 같은 번호를 가리키므로 목록을 고치면 함께 바뀐다). 수식 셀에는 계산 결과가 함께 저장된다.
- [x] 03. EPUB (pages/03-epub.md) — 2026-08-08
  - 확인 방법: 저작권이 풀린 무료 전자책을 내려받아 압축을 풀어 대조했다. `mimetype`이 맨 앞·무압축(01장과 같은 규칙), `META-INF/container.xml`이 `content.opf`를 가리키고, 본문은 진짜 XHTML에 `<link rel="stylesheet">`까지 붙어 있다.
- [x] 04. SVG (pages/04-svg.md, assets/svg-before-after.png) — 2026-08-08. **제2부 시작**(pages/part-2.md)
  - 확인 방법: SVG를 직접 만들어 브라우저로 렌더링하고, 사명·색을 문자열 치환한 뒤 다시 렌더링해 그림이 바뀌는 것을 확인했다. 그 before/after가 assets/svg-before-after.png.
  - 실습이 "여는 것"이 아니라 "메모장으로 만드는 것"이라 앞 장들과 결이 다르다.
- [x] 05. CSV (pages/05-csv.md) — 2026-08-08
  - 확인 방법: 같은 명단을 UTF-8 / UTF-8+BOM / CP949로 각각 저장해 Excel COM으로 열어 대조했다. BOM 3바이트(163→166)가 깨짐과 정상을 가른다. 엑셀 자동변환도 실측: `00123`→`123`, `3-1`→**2026년 3월 1일**, 16자리 `...3456`→`...3450`(값 손상, 15자리까지는 안전).
- [x] 06. JSON (pages/06-json.md) — 2026-08-08
  - 확인 방법: 여러 형태를 실제로 파싱해 무엇이 통과하고 무엇이 오류인지 대조했다. 마지막 쉼표·작은따옴표·주석·따옴표 없는 키·`00123`(0으로 시작하는 숫자)은 전부 오류. `"00123"`과 16자리 숫자는 그대로 보존 → 05장에서 엑셀이 망가뜨리던 값이 여기선 산다는 것이 이 장의 축.
  - 크롬은 JSON을 정리해 주지도, 오류를 알려 주지도 않는다(직접 확인). 그래서 실습을 "AI에게 검사받기"로 짰다.
- [x] 07. XML (pages/07-xml.md) — 2026-08-08
  - 확인 방법: 크롬이 XML을 트리로 펼쳐 보여 주고 오류는 줄·칸까지 짚어 준다(JSON과 정반대). `&`를 그대로 쓰면 not well-formed 오류 → `&amp;` 필요.
  - 훅: 우리나라 전자세금계산서는 법으로 XML 파일 자체를 주고받는다(국세청). 1~4장에서 만난 `<hp:t>`·`<w:t>`·`<a:t>`·`<circle>`을 여기서 회수한다.
- [x] 08. YAML (pages/08-yaml.md) — 2026-08-09, **2026-08-09 전면 재작성**
  - 첫 원고는 "자동화 도구의 설정입니다" 같은 일반론뿐이라 비전공자에게 와닿지 않았다(사용자 지적). 총무팀 김 대리의 사내 안내 챗봇 설정 파일을 이야기의 축으로 삼아 다시 썼다. 제목도 `괄호를 없앴더니 생긴 일` → `회사 설정 파일을 직접 고치는 법`으로 교체.
  - 실측한 사고: 탭 들여쓰기는 오류, `회사:한빛물산`(콜론 뒤 공백 없음)은 오류가 아니라 통글자가 됨, `내선: 0105` → **69**(8진수), `자동응답: no` → **거짓**, `버전: 1.10` → **1.1**, `사물함: 12:30` → **750**(초 환산). 해법은 05장과 동일하게 큰따옴표.
  - 핵심 각도: 사무직은 YAML을 만들지 않고 **받아서 고친다**. "챗봇이 이상해요" 대신 "15번째 줄 내선번호에 따옴표가 빠졌습니다"라고 말할 수 있게 되는 것이 이 장의 값어치.
- [x] 09. MD (pages/09-md.md) — 2026-08-09
  - 훅: 이 책 자체가 .md로 쓰였고 원고 하나에서 위키독스·인쇄본·전자책이 나온다. AI가 이미 마크다운으로 답한다는 점이 자동화의 핵심.
- [x] 10. ICS (pages/10-ics.md) — 2026-08-09
  - 회의 하나 = 328바이트(실측). 시각 끝의 `Z`는 세계 표준시라 한국 시간과 9시간 차이. 같은 `UID`로 다시 보내면 기존 일정이 갱신된다. 줄바꿈은 CRLF 규격.
- [x] 11. VCF (pages/11-vcf.md) — 2026-08-09. **제2부 완료**
  - 명함 한 장 = 194바이트(실측). `N:성;이름;;;` 순서와 `FN`(표시용 이름)을 둘 다 채워야 한국 이름이 제대로 뜬다. 두 글자 성(남궁·황보) 예외 처리를 AI 지시문에 명시. 개인정보 취급 주의를 강하게 적었다.
- [ ] **다음 장: 사용자가 고른 뒤 착수** (아래 "다음 장 정하기" 참고)
  - 11장 끝은 제3부(사진·소리·영상, 열어도 글자가 아닌 것)를 예고하며 닫았다. 다음 부/장 구성은 아래 "부 구성 재편안" 참고.

- [x] 12. HEIC · WebP · AVIF (pages/12-heic-webp-avif.md) — 2026-08-09. **제3부 시작**(pages/part-3.md)
  - 셋을 한 장에 묶었다. 머리말의 `IMG_1234.HEIC` 장면을 여기서 회수한다.
  - **측정 주의**: 형식마다 "품질 80"의 뜻이 달라 같은 숫자로 저장해 비교하면 HEIC가 JPG보다 커진다(실제로 그렇게 나왔다). 반드시 **화질(PSNR)을 맞춰 놓고** 크기를 비교할 것. 맞춰서 재면 JPG 100% / WebP 67% / AVIF 50% / HEIC 50%.
  - 3부의 원리: 2부는 확장자를 바꾸면 열렸지만(실제로 그 형식이었으니까), 미디어는 파일 맨 앞의 시그니처가 정체를 정하므로 **이름만 바꿔도 변환이 아니다**. HEIC를 .jpg로 바꿔도 내용은 HEIF 그대로(실측).

- [x] 13. MP4 · WebM (pages/13-mp4-webm.md) — 2026-08-09
  - 축은 **컨테이너와 코덱**이다. 같은 영상을 H.264와 H.265로 인코딩해 둘 다 .mp4로 저장한 뒤 ffprobe로 확인했다(실측). "같은 mp4인데 왜 안 되죠"의 답.
  - 720p 10초: 무압축 120.5MB → H.264 1.2MB / VP9 0.6MB / H.265 0.4MB. **단, 사진 줌 영상이라 움직임이 단순해 유난히 잘 줄었다. 실제 촬영본은 더 크다는 단서를 본문에 달았다.**
- [x] 14. MP3 · WAV · FLAC (pages/14-mp3-wav-flac.md) — 2026-08-09
  - 1분 기준 실측: WAV 10.1MB / FLAC 3.6MB(36%) / MP3 128k 0.9MB(9%) / M4A 0.9MB. 한 시간이면 606MB vs 55MB.
  - 이 장의 심장은 **회의 녹음 → AI 전사 → 회의록**이다. 지시문에 "지어내지 마라, 발언자를 추측하지 마라, 모르면 (확인 필요)로 적어라"를 넣었다. 녹음 동의와 외부 서비스 업로드 주의도 강하게 적었다.
- [x] 15. RAW (pages/15-raw.md) — 2026-08-09. **제3부 완료**
  - 실제 캐논 CR2를 받아 확인: 34.2MB, 센서 4992×3292, 14비트(최대값 15522), 현상한 JPG는 3.15MB → **RAW이 JPG의 10.9배**. 파일 시그니처 `4D 4D 00 2A`는 TIFF.
  - 사무직 각도: 외주 촬영 원본을 받았는데 안 열린다. 해법의 절반은 "보정본을 웹용 JPG로, 원본 RAW도 함께"라고 **요청할 줄 아는 것**.

- [x] 04. ODT · ODS · ODP (pages/04-odf.md) — 2026-08-09. **1부에 끼워 넣음**
  - 실측: 엑셀·워드·파워포인트 COM으로 실제 ODF를 저장해 확인했다. 셋 다 첫 항목이 `mimetype`이고 무압축(01장 한글, 03장 EPUB과 같은 규칙). 본문은 전부 `content.xml` 하나(오피스는 흩어져 있다). 항목 수 5~7개로 docx의 17개보다 훨씬 단순. 텍스트 태그는 `<text:p>`.
  - 실무 핵심: **마이크로소프트 오피스가 ODF를 그냥 연다.** 협력업체가 .ods를 보내와도 당황할 일이 없다.
- [x] 05. PDF (pages/05-pdf.md) — 2026-08-09. **1부 마무리**
  - 실측: 이 책 인쇄본 PDF는 `%PDF-1.4`로 시작하고 글자가 좌표와 함께 들어 있다(22쪽에서 314자 추출). 같은 쪽을 사진으로 찍어 PDF로 만드니 **추출 0자, 한 쪽에 4.4MB**(글자 PDF는 101쪽 전체가 8.2MB).
  - 이 장의 축은 "PDF는 그림이 아니다". 그리고 검사하기 사례로 **검은 칠 마스킹 사고**를 넣었다(도형을 얹은 것일 뿐 글자는 살아 있어 복사하면 나온다).

- [x] 18. HTML (pages/18-html.md) — 2026-08-09. **제4부 시작**(pages/part-4.md)
  - 실측: HTML 460바이트 / CSS 480바이트 / JS 163바이트로 화면 하나가 완성된다.
  - 이 부의 축은 **17장 내내 써 온 "브라우저에서만 도는 HTML 파일 하나를 만들어 줘"의 회수**다. 왜 그 조건이 데이터를 지켰는지를 여기서 설명한다.
- [x] 19. CSS (pages/19-css.md, assets/css-before-after.png) — 2026-08-09
  - 그림: 같은 index.html에 style.css만 연결한 before/after를 실제로 렌더링해 만들었다.
  - 실무 포인트: 색·크기를 파일 맨 위에 이름 붙여 모아 두라고 지시하면 나중에 사용자가 직접 한 줄만 고칠 수 있다. 글꼴을 인터넷에서 받아 오게 하면 사내망에서 깨진다.
- [x] 20. JS (pages/20-js.md) — 2026-08-09. **제4부 완료**
  - 실측: 세 줄(163바이트)짜리 스크립트로 버튼을 눌렀을 때 빈 자리에 글자가 나타나는 것을 확인했다.
  - 메시지: 비개발자가 JS를 배울 필요는 없다. AI가 만든 것을 열어 **어디를 고칠지 짚을 수 있으면** 된다. 계산 도구는 반드시 손으로 검산하고, 지시문에 "계산 과정을 한 줄씩 보여 달라"를 넣는다.

- [x] **제5부 완료** — 21 PARQUET·JSONL / 22 GGUF·SAFETENSORS / 23 IPYNB (2026-08-09)
  - 21: 20만 줄 실측. CSV 7.3MB → Parquet 2.6MB(36%). 전체 읽기는 큰 차이 없었지만 **칸 하나만 읽으면 0.01초 대 0.26초**. 엑셀 한 시트 104만 줄 한도를 지시문 조건으로 넣었다.
  - 22: safetensors를 만들어 확인 — 맨 앞 8바이트가 머리말 길이, **그 머리말이 JSON**(8장 회수). `safe`의 뜻은 "여는 것만으로 실행되지 않는다".
  - 23: ipynb도 **JSON**이다. `cells`에 markdown/code가 섞이고 **결과까지 저장**된다. 그래서 "화면의 결과가 지금 코드의 결과라는 보장이 없다"는 함정을 다뤘다.
- [x] **제6부 완료** — 24 ZIP / 25 APK·JAR·IPA / 26 ISO·TORRENT (2026-08-09)
  - 24: 압축률 실측이 킬러다. 텍스트 16,000→**217바이트(1.4%)**, JPG **99.2%**, docx 93.6%. "사진 폴더 압축해도 소용없다"가 여기서 증명된다. 암호를 걸어도 **파일 이름 목록은 보인다**는 것도 실무 핵심.
  - 25: 진짜 JAR(Android Studio)을 zipfile로 열어 확인 — 첫 항목이 `META-INF/MANIFEST.MF`. 서명 때문에 고치면 설치가 안 된다는 점을 "1부의 방법이 통하지 않고, 통하지 않는 것이 옳다"로 정리.
  - 26: **실물 검증 없이 쓴 유일한 장.** ISO/토렌트 샘플을 구하지 않았다. 나중에 보강 여지 있음. 토렌트는 저작권·사내 규정 주의를 분명히 적었다.
- [x] **제7부 완료, 본문 28장 완결** — 27 열기 대화상자 / 28 옛 포맷 (2026-08-09)
  - 27: 사용자가 보내 준 엑셀·워드·한글 열기 화면 3장이 소재. 목록을 **네 지층**(현재 형식 / 자기 집안 옛것 / 남의 집 것 / 유물)으로 읽는 법. `.prn`·`.slk`·`.dif`는 사실 글자 파일이라 메모장으로 열린다.
  - 28: 실측 — `.doc`과 `.xls`의 시그니처가 **똑같이 `D0 CF 11 E0`**(같은 OLE 상자). `.rtf`는 `{\rtf1`로 시작하는 글자. 옛 형식의 진짜 위험은 매크로이고, 그래서 새 형식은 `m`이 붙은 별도 확장자로 갈라 두었다.
  - 28장 끝에서 책 전체를 닫았다.

## 자동화 사례의 네 가지 유형 (2026-08-09 추가)
사례가 "찍어내기"와 "뽑아내기"에 몰려 아이디어가 빈약하다는 지적을 받았다. 각 장에서 유형을 의식적으로 섞는다.
1. **찍어내기** — 서식 하나로 문서 수백 장 (01, 02, 04, 10, 11)
2. **뽑아내기** — 흩어진 파일에서 값만 꺼내 표 하나로 (02, 05, 06, 07)
3. **바꿔치기** — 문서 뭉치에서 사명·색을 일괄 교체 (02, 04)
4. **검사하기** — 다 만든 뒤 잘못된 것 찾기, 내보내기 전 민감정보 걸러내기 (01, 02, 05)
**4번이 가장 자주 빠지고 가장 값어치가 크다.** 앞의 셋은 사람이 오래 걸려서 하던 일이지만 검사는 사람이 아예 못 하던 일이다. 서문(00-intro)에 이 네 유형을 설명해 두었으니 각 장은 그 틀 안에서 사례를 고른다.

## 부 구성 (2026-08-09 확정)
1. **열어보면 다른 것** (압축 상자) — hwpx, docx·xlsx·pptx, epub ✓ 완료
2. **열어보면 글자인 것** (텍스트) — svg, csv, json, xml, yaml, md, ics, vcf ✓ 완료
3. **눈과 귀에 담기는 것** (미디어) — heic·webp·avif ✓, 이후 webm, flac, raw(cr2/nef) 등
4. **만들면서 배우는 것** (바이브코딩) — html, css, js. 자매편 『브라우저의 재발견』과 겹치지 않게 "파일로서의 정체"에 초점
5. **AI와 데이터의 것** — parquet, jsonl, gguf, safetensors, ipynb
6. **통째로 든 것** (묶음) — zip, apk, jar, ipa, iso, torrent
7. **목록에만 있던 것** (프로그램이 여는 확장자들) — 열기 대화상자 읽는 법, ods·odt·rtf, 옛 포맷(doc/xls/ppt/hwp). 확장자 하나에 한 장씩 쓰면 얕아지는 것들을 묶어 다루는 부. 책 후반 정리 성격이라 뒤쪽에 둔다.

## 중간에 새 장 끼워 넣기
사용자가 갑자기 확장자를 지목하면 **해당 부의 맞는 자리에 넣는다.** 절차는 이렇다.
1. `pages/`에 원고를 아무 번호로나 만든다 (예: `99-zip.md`)
2. `TOC.md`의 원하는 자리에 줄을 추가한다
3. `python print/renumber.py --apply` — 파일명·H1 번호·TOC 경로를 자동으로 다시 맞춘다
4. 스크립트가 출력해 주는 `**다음 장 →**` 줄들과, 다음 장 도입부의 연결 문장을 손으로 고친다 (내용이라 기계가 못 한다)

## 이전 재편안 (참고용, 위 확정안으로 대체됨)
집필이 진행되며 애초 4부 계획과 실제가 어긋났다(JSON이 2부로 편입). 성격 기준으로 다시 묶는 안:
1. **열어보면 다른 것** (압축 상자) — hwpx, docx·xlsx·pptx, epub ✓ 완료
2. **열어보면 글자인 것** (텍스트) — svg, csv, json, xml, yaml, md, ics, vcf ✓ 완료
3. **눈과 귀에 담기는 것** (미디어) — webp, avif, heic, raw(cr2/nef), flac, webm
4. **AI와 데이터의 것** — parquet, jsonl, gguf, safetensors, ipynb
5. **통째로 든 것** (묶음) — zip, apk, jar, ipa, iso, torrent
6. **바이브코딩의 것** — html, css, js (+ 사용자 요청). 자매편 『브라우저의 재발견』과 겹치지 않게 "파일로서의 정체"에 초점.
사용자 요청 추가 검토: 한글/엑셀/워드가 여는 확장자 전수(ods, odt, rtf, dbf, slk, dif, prn, hwt/hwtx, cell, show 등)를 어떻게 다룰지. 장마다 한 개씩 쓰기엔 얕고, "프로그램이 여는 목록" 자체를 한 장으로 다루는 방식이 후보.

## 다음 장 정하기 (중요)
**다음에 무슨 확장자를 쓸지 임의로 정하지 않는다.** 한 장을 끝내면 후보 2~3개를 이유와 함께 제시하고 사용자가 고르게 한다. 각 장의 마지막 "다음 장" 연결 문단도 그 결정이 난 뒤에 쓴다.

## 부·장 순서 바꾸기
나중에 순서를 바꿀 수 있다. 바꿀 때 손대야 할 곳은 네 군데다.
1. `pages/NN-slug.md` 파일명의 번호 접두사
2. 그 파일의 H1 `# NN. 제목`
3. `TOC.md`의 해당 줄 위치와 제목 (위키독스 계층은 전적으로 이 파일을 따른다)
4. **앞 장 끝의 `**다음 장 →**` 줄과 다음 장 도입부의 연결 문장** — 여기가 실제로 손이 가는 곳이다
인쇄본은 `TOC.md` 순서를 그대로 읽으므로 빌드는 손댈 필요가 없다.

## 인쇄본·전자책 빌드
`python print/build.py --pdf` 한 줄이면 `TOC.md` 순서대로 원고를 읽어 크라운판 HTML과 PDF를 만든다.
새 장을 쓰고 TOC.md에 등록하면 자동 포함되므로 빌드 스크립트는 손댈 필요가 없다.
`--only 03`은 집필 중 그 장만 가볍게 미리보기. 폰트·Paged.js 원본은 `print/assets/`에 있다.
PDF는 Paged.js 조판이 끝날 때까지 기다렸다가 뽑는다(그냥 헤드리스 인쇄하면 앞 몇 쪽만 나온다).

**새 장 집필 전 원칙**: 내부 구조는 추측하지 말고 실제 파일을 만들어 열어보고 쓴다. 01·02가 그렇게 쓰였다.

## 산출물 3종 (원고 하나, 조판 셋)
1. **위키독스** — `main` push 시 자동 발행 (`TOC.md`·`pages/`·`assets/`)
2. **인쇄본** — 크라운판 176×248mm PDF (`print/`)
3. **전자책** — 자체완결 HTML + PDF (`print/`)
원고는 `pages/*.md` 하나이며 조판만 다르다. 장을 새로 쓰면 세 산출물을 모두 갱신 대상으로 본다.

**표지 확정**: `assets/cover.png` (크림·플럼 편집형, 1744×2336). 위키독스 업로드용 100×130 판본은 `assets/cover-100x130.png`. 인쇄본·전자책 표지도 같은 원본을 쓴다.

## GitHub 저장소 (완료)
- 원격: **https://github.com/cdsassj00/hwakjangja-book** (공개, 기본 브랜치 `main`)
- 2026-08-08 첫 push 완료(머리말 + 01. HWPX + 인쇄 마스터). 이후로는 아래 "새 장 추가 워크플로우"만 반복한다.
- 위키독스 연결은 위키독스 웹에서 **새 책 만들기 → 깃허브 연동**에 이 저장소 URL을 등록하면 되고, 연결 후에는 `main` push마다 자동 반영된다. (연결은 사람이 직접 수행 — 위키독스 계정 작업)

## 새 장 추가 워크플로우 (반복)
1. `pages/NN-slug.md` 작성(H1 `# NN. 제목`), 필요 그림은 `assets/`에.
2. `TOC.md`에 한 줄 추가(위치 맞게).
3. 커밋 후 push → 위키독스 자동 반영.
```bash
git add . && git commit -m "docs: add ch NN. 제목" && git push
```

## 참고: 인쇄용 책(별도 산출물)
위키독스와 별개로, 인쇄판은 **크라운판 176×248mm, 대칭 여백**, Pretendard 내장 자체완결 HTML → PDF(3mm 블리드+재단선) 파이프라인으로 만든다. 원고는 동일하되 조판만 다르다.
