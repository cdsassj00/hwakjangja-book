# CLAUDE.md — 이 저장소에서 Claude Code가 따를 지침

## 이 저장소가 하는 일
비개발자용 실용 교양서 **『확장자의 재발견』**의 원고 저장소다. 낯선 파일 확장자를 하나씩 열어 정체를 밝히고, 그 구조로 **무엇을 자동화할 수 있는지**, 그 일을 **AI에게 어떻게 시키는지**까지 다룬다. (자매편: 『브라우저의 재발견』)

이 저장소는 위키독스와 연동된다: **`main`에 push하면 위키독스 책에 자동 반영**된다. 규칙은 [github-wikidocs](https://github.com/pahkey/github-wikidocs)를 따른다.

## 저장소 구조 · 규칙
- `TOC.md` — 목차(위키독스 페이지 계층). 모든 페이지를 여기 등록해야 노출됨. `*` 리스트 + 2칸 들여쓰기, 제목은 페이지 H1과 정확히 일치.
- `pages/NN-slug.md` — 각 장. 번호 접두사 + kebab-case. H1은 `# NN. 제목`.
- `assets/` — 이미지. 페이지에서 `../assets/파일.png`로 참조(현재 검증된 경로).
- 상세 작성 규칙은 `AGENTS.md` 참고(제목/리스트/코드블록/이미지 규약).
- 위키독스 박스는 `[[TIP("제목")]] … [[/TIP]]`만 안전하게 지원됨. 그 외 콜아웃은 인용문/표로.

## 책의 톤·챕터 템플릿 (모든 장 공통)

**이건 책이다. 자료집도 슬라이드도 아니다.** 독자는 문단을 따라 읽는다. 훑지 않는다.

- 대상: **비개발자**. 문체 합니다체. 짧은 문장을 쓰되 **문단으로 흐르게** 쓴다. 비유로 원리를 대체하지 않는다. **보장하지 않는 것**을 반드시 명시한다.
- **산문이 기본값이다.** 표·목록·박스는 산문으로 쓰면 오히려 읽기 나쁜 정보에만 쓴다. 설명할 수 있으면 문장으로 쓴다.
- 한 장당 상한: **표 1개, `[[TIP]]` 박스 1개(실습), 코드블록 2~3개(AI 지시문), 소제목 5~6개.** 넘으면 자료집이 된다.
- 금지: 신상명세 표, 사람 vs 구조 비교표, 「3줄 요약」 같은 개조식 마무리, 장식용 인용구 남발. 요약이 필요하면 마지막 문단이 그 일을 한다.
- 흐름(형식이 아니라 서사): 마주친 장면 → 정체를 한 문장으로 → 직접 열어보기 → 그래서 무엇을 자동화하나 → AI에게 시키는 법과 한계 → 같은 열쇠로 열리는 다음 문.
- **독자 등급은 "완전 초보 일반인"이다.** 사무직 직장인이 읽는다. 전문용어는 처음 나올 때 반드시 한 문장으로 풀고, 영어 약어에는 우리말 설명을 붙인다. "아시다시피", "당연히" 같은 전제 금지. 어려운 개념이 나오면 그 자리에서 예를 든다.
- **모든 장이 반드시 답해야 하는 세 가지** (빠뜨리면 미완성):
  1. **무엇으로 열고 무엇으로 고치나** — 그 확장자를 열람·편집할 수 있는 프로그램. 유료만 말하지 말고 무료·기본 내장 수단을 꼭 포함한다. 초보자가 이 문단만 읽고 당장 파일을 열 수 있어야 한다.
  2. **이 확장자로 어떤 업무자동화를 할 수 있나** — 직무 사례로.
  3. **주로 어떤 결과물을 만들고, 그중 AI에게 넘길 수 있는 지점이 어디인가** — 사람이 판단할 부분과 AI가 반복할 부분을 갈라 준다.
- **자동화 절은 이 책의 심장이다.** "이런 걸 할 수 있습니다" 같은 일반론으로 끝내지 않는다. 독자는 사무직 직장인이므로 **직무와 산출물을 이름으로 부른다** — 인사팀의 재직증명서 200장, 총무팀의 위촉장, 회계팀의 부서별 예산 취합, 영업팀의 지점 실적 보고서, 기획팀의 제안서 사명 일괄 교체처럼. 각 사례는 "지금 사람이 어떻게 하는가 → 구조를 알면 어떻게 바뀌는가 → 그 일을 생성형 AI에게 어떤 말로 시키는가"까지 내려간다. 사례는 장마다 2~3개면 충분하되 반드시 구체적이어야 한다.
- 기준 구현: `pages/01-hwpx.md`. 새 장은 이 **밀도와 문체**를 따른다(구조를 기계적으로 복사하지 않는다).

## 100개 확장자 4개 부(部) 계획
1. 숨은 정체가 반전인 것: hwpx, docx, xlsx, pptx, epub, apk, jar, ipa (=ZIP+XML/묶음)
2. 일상에서 마주치는 낯선 것: svg, md, yaml, toml, ics, vcf, torrent, iso, csv
3. AI·데이터 시대 포맷: json, jsonl, parquet, gguf, safetensors, ipynb, webp, avif
4. 미디어·창작 포맷: heic, raw(cr2/nef), flac, webm, gltf/glb, psd

## 진행 상태
- [x] 01. HWPX (pages/01-hwpx.md, assets/hwpx-structure.png) — 2026-08-08 산문 중심으로 전면 리라이트
- [x] 02. DOCX · XLSX · PPTX (pages/02-docx-xlsx-pptx.md) — 2026-08-08
  - 집필 전 python-docx·openpyxl·pptx와 Excel COM으로 실제 파일을 만들어 내부를 확인했다. 검증 스크립트는 세션 스크래치패드에 있었고 저장소에는 넣지 않았다.
  - 확인된 사실: 오피스는 `mimetype`이 없고 `[Content_Types].xml`을 쓰며 항목 순서 제약이 없다. 엑셀은 문자열을 `xl/sharedStrings.xml`에 모아 두고 셀에는 번호만 넣는다(같은 글자를 쓴 두 셀이 같은 번호를 가리키므로 목록을 고치면 함께 바뀐다). 수식 셀에는 계산 결과가 함께 저장된다.
- [x] 03. EPUB (pages/03-epub.md) — 2026-08-08
  - 확인 방법: 저작권이 풀린 무료 전자책을 내려받아 압축을 풀어 대조했다. `mimetype`이 맨 앞·무압축(01장과 같은 규칙), `META-INF/container.xml`이 `content.opf`를 가리키고, 본문은 진짜 XHTML에 `<link rel="stylesheet">`까지 붙어 있다.
- [ ] **다음: 04. SVG — 메모장으로 열리는 그림**
  - 03의 마지막에서 "웹페이지가 글자로 화면을 그린다면 그림 자체를 글자로 적은 것도 있다"로 이어진다. 회사 로고가 svg로 와서 그림판으로 안 열리는 장면에서 출발.
- [ ] 이후 CSV, JSON, YAML …

## 인쇄본·전자책 빌드
`python print/build.py --pdf` 한 줄이면 `TOC.md` 순서대로 원고를 읽어 크라운판 HTML과 PDF를 만든다.
새 장을 쓰고 TOC.md에 등록하면 자동 포함되므로 빌드 스크립트는 손댈 필요가 없다.
`--only 03`은 집필 중 그 장만 가볍게 미리보기. 폰트·Paged.js 원본은 `print/assets/`에 있다.
PDF는 Paged.js 조판이 끝날 때까지 기다렸다가 뽑는다(그냥 헤드리스 인쇄하면 앞 몇 쪽만 나온다).

**새 장 집필 전 원칙**: 내부 구조는 추측하지 말고 실제 파일을 만들어 열어보고 쓴다. 01·02가 그렇게 쓰였다.

## 산출물 3종 (원고 하나, 조판 셋)
1. **위키독스** — `main` push 시 자동 발행 (`TOC.md`·`pages/`·`assets/`)
2. **인쇄본** — 크라운판 176×248mm PDF (`print/`)
3. **전자책** — 자체완결 HTML + PDF (`print/`)
원고는 `pages/*.md` 하나이며 조판만 다르다. 장을 새로 쓰면 세 산출물을 모두 갱신 대상으로 본다.

**표지 확정**: `assets/cover.png` (크림·플럼 편집형, 1744×2336). 위키독스 업로드용 100×130 판본은 `assets/cover-100x130.png`. 인쇄본·전자책 표지도 같은 원본을 쓴다.

## GitHub 저장소 (완료)
- 원격: **https://github.com/cdsassj00/hwakjangja-book** (공개, 기본 브랜치 `main`)
- 2026-08-08 첫 push 완료(머리말 + 01. HWPX + 인쇄 마스터). 이후로는 아래 "새 장 추가 워크플로우"만 반복한다.
- 위키독스 연결은 위키독스 웹에서 **새 책 만들기 → 깃허브 연동**에 이 저장소 URL을 등록하면 되고, 연결 후에는 `main` push마다 자동 반영된다. (연결은 사람이 직접 수행 — 위키독스 계정 작업)

## 새 장 추가 워크플로우 (반복)
1. `pages/NN-slug.md` 작성(H1 `# NN. 제목`), 필요 그림은 `assets/`에.
2. `TOC.md`에 한 줄 추가(위치 맞게).
3. 커밋 후 push → 위키독스 자동 반영.
```bash
git add . && git commit -m "docs: add ch NN. 제목" && git push
```

## 참고: 인쇄용 책(별도 산출물)
위키독스와 별개로, 인쇄판은 **크라운판 176×248mm, 대칭 여백**, Pretendard 내장 자체완결 HTML → PDF(3mm 블리드+재단선) 파이프라인으로 만든다. 원고는 동일하되 조판만 다르다.
